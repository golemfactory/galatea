import { Heading } from '../index';
import { StyledPlaceholder } from './styles';

const Placeholder = () => (
  <StyledPlaceholder>
    <Heading>Please select some text...</Heading>
    So we can analyze it
  </StyledPlaceholder>
);

export default Placeholder;
