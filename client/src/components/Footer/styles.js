import styled from 'styled-components';

export const StyledFooter = styled.div`
  height: 4rem;

  position: absolute;
  left: 0;
  right: 0;
  bottom: 0;

  display: flex;
  justify-content: flex-end;
  align-items: center;

  padding: 2rem;

  img {
    margin-left: 0.5rem;
  }
`;
